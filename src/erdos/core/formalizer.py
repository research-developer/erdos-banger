"""Lean skeleton generator (stub)."""

from pathlib import Path

from erdos.core.models import ProblemRecord


def generate_skeleton(problem: ProblemRecord, project_path: Path) -> Path:
    raise NotImplementedError("Feature not yet implemented (requires Spec 007)")
