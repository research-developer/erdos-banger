# erdos-banger environment configuration
# Copy to .env and fill in your values

# =============================================================================
# LLM PROVIDERS (for erdos ask/loop commands)
# =============================================================================

# OpenAI API (for scripts/llm-openai.sh)
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-5.2
# Reasoning effort: none, low, medium, high, xhigh
OPENAI_REASONING_EFFORT=xhigh

# Anthropic API (for scripts/llm-anthropic.sh)
ANTHROPIC_API_KEY=sk-ant-your-key-here
ANTHROPIC_MODEL=claude-opus-4-5-20251101

# Google API (for Gemini - used by Marker PDF LLM mode)
# GOOGLE_API_KEY=your-gemini-key

# Default LLM command (used by erdos ask/loop)
ERDOS_LLM_COMMAND=./scripts/llm.sh

# =============================================================================
# FORMAL PROVERS (Optional)
# =============================================================================

# Harmonic Aristotle (Lean prover)
#
# Setup:
# - Install: `uv pip install aristotlelib` (or use your preferred env manager)
# - Set ARISTOTLE_API_KEY in your local `.env` (never commit real keys)
#
# Docs: see `docs/_vendor-docs/harmonic-aristotle/README.md`
ARISTOTLE_API_KEY=arstl-your-key-here

# Optional: command used by future Aristotle integration (SPEC-021).
# If not set, implementations should default to `aristotle` on PATH.
ERDOS_ARISTOTLE_COMMAND=aristotle

# =============================================================================
# ACADEMIC METADATA APIS
# =============================================================================

# Email for "polite pool" - faster responses from OpenAlex/Crossref
ERDOS_MAILTO=your-email@example.com

# OpenAlex (Primary - 271M+ works, 100k requests/day)
# API key is OPTIONAL but gives you authenticated access and higher limits
# Get your key at: https://openalex.org/users/me (sign in required)
# Docs: https://docs.openalex.org/
OPENALEX_API_KEY=your-openalex-api-key-here
# Optional: PyAlex config alias (Spec 020)
# OPENALEX_EMAIL=your-email@example.com

# arXiv API (Secondary - for LaTeX source)
# No API key required! Rate limit: 1 request/3 seconds
# Docs: https://info.arxiv.org/help/api/

# Crossref API (Fallback - for DOI metadata)
# No API key required! Add email above for polite pool.
# Docs: https://www.crossref.org/documentation/retrieve-metadata/rest-api/

# =============================================================================
# RESEARCH APIS (Future - SPEC-029+)
# =============================================================================

# Exa Research API (Agentic literature synthesis)
# Get your key at: https://exa.ai/ (sign in required)
# Docs: https://docs.exa.ai/
# Used by: future `erdos research exa` command for structured research queries
EXA_API_KEY=your-exa-api-key-here

# Semantic Scholar API (Citation context extraction)
# Get your key at: https://www.semanticscholar.org/product/api
# Docs: https://api.semanticscholar.org/api-docs/
# Used by: future citation graph + "why does X cite Y" queries
SEMANTIC_SCHOLAR_API_KEY=your-semantic-scholar-api-key-here

# zbMATH Open API (Math-specific metadata)
# No API key required! MSC codes, math reviews, equation search.
# Docs: https://api.zbmath.org/
# Used by: future math-specific classification and search

# =============================================================================
# PDF CONVERSION (Optional - spec-019, v2.0+)
# =============================================================================

# Marker LLM backend selection (when using --use-llm flag)
# Options: gemini (default), claude, openai, ollama, vertex
# Uses existing API keys above (GOOGLE_API_KEY, ANTHROPIC_API_KEY, OPENAI_API_KEY)

# For local LLM via Ollama (no API costs):
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2

# PyTorch device override (auto-detected by default)
# TORCH_DEVICE=cuda    # NVIDIA GPU
# TORCH_DEVICE=mps     # Apple Silicon
# TORCH_DEVICE=cpu     # CPU only
